# Leave blank when the API is served from the same origin.
# When hosted separately, set this to the temporary or permanent backend URL.
VITE_API_BASE_URL=

# Location of exported result data.
VITE_SNAPSHOT_BASE_URL=/snapshots
